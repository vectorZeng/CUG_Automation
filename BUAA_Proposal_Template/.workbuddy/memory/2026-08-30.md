# 2026-08-30 文献综述格式调整

- 修改 `gbt7714-numerical.bst`：
  - `show.doi := #0`，彻底去掉参考文献中的 DOI。
  - `show.medium.type := #0`，`[J/OL]` 改为 `[J]`。
- 修改 `buaascseproposal.cls`：
  - `section` 标题格式改为 `\zihao{4}\heiti\bfseries`（四号加粗）。
  - `subsection` 标题格式改为 `\zihao{-4}\heiti\bfseries`（小四加粗）。
- 重新编译 `xelatex → bibtex → xelatex × 2`，生成 39 页 PDF，无未定义引用，参考文献无 J/OL、无 DOI。

## 下午补充修改（2026-08-30 17:15）
- `buaascseproposal.cls` `ctexset`：
  - `section` 的 `beforeskip`/`afterskip` 均改为 `1\baselineskip`（段前段后各 1 行）。`subsection` 不变。
- 目录页去掉页码但保留页眉：新增 `tocmatter` 页样式（页眉居中黑色论文题目、页脚留空），`content/toc.tex` 由 `frontmatter` 改为 `tocmatter`，之后切回 `mainmatter`。
- 目录中"摘要"条目（第一条，由 `\addcontentsline{toc}{section}{\cabstractname}` 写入）上方空 0.5 行：`content/abstract.tex` 在该行前加 `\addtocontents{toc}{\vspace{0.5\baselineskip}}`。
- 重新编译通过。

## 17:29 用户决定：目录页恢复原格式（撤销间距注入）
- 实测确认：摘要条目是目录第一条，其"前面"的 `\vspace` 会被"目录"标题默认大间距 + `\l@section` 的 `\addvspace` 合并吞掉，肉眼看不出空行（坐标实测：目录标题 y≈46.9、摘要条目 y≈130.3、引言 y≈155.2，间距恒定不变）。
- 用户要求"目录页不要动，恢复到之前的格式"：已删除 `content/abstract.tex` 中 `\addtocontents{toc}{...}` 那行，目录回到 `tocmatter`（无页码、留页眉）且无注入间距的原格式。
- 结论：目录页保持不动，不再尝试在摘要条目前加空行。

## 17:44 生成文献综述汇报 PPT
- 基于 `content/related_work.tex` 的问题牵引式文献综述，参考 `E:\filereceive\1_陈晓翀-第二次文献综述PPT.pptx` 风格生成 16:9 汇报 PPT。
- 复用参考模板的顶部学校 logo 条、深蓝色标题、横线、目录过渡页布局、页脚页码等视觉元素。
- 内容结构：封面 + 目录 + 引言 + **研究主线页** + 5 章过渡页 + 5 个内容页（场景任务、环境感知、多模态融合、监督缺陷检测、无监督异常检测、多模态异常认知、巡检规划、多机协同）+ 总结与展望 + 致谢，共 19 页。
- 嵌入现有综述插图：FAST-LIO2 轨迹、Faster R-CNN 模型、PatchCore 异常样例、DRAEM 结果、RRT* 路径、多机协同飞行图等。
- 新增“研究主线：感知—认知—决策闭环”一页，用箭头连接感知/认知/决策三大模块并加反馈回路。
- 输出：`outputs/无人机自主巡检文献综述_汇报.pptx`。

## 20:15 统一英文缩写首现格式
- 要求：英文缩写首次出现时按"中文全称（英文全称, 缩写）"格式，如"无人机（Unmanned Aerial Vehicle, UAV）"。
- 在 `content/related_work.tex` 与 `content/abstract.tex` 中，对全文首次出现的缩写按此格式展开，共 11 处：
  - 无人机 UAV（摘要第1次 + 正文第4行各1次）、全球导航卫星系统 GNSS、全球定位系统 GPS、惯性测量单元 IMU、激光雷达 LiDAR、视觉里程计 VO、同步定位与建图 SLAM、红绿蓝 RGB、建筑信息模型 BIM、下一最佳视点 NBV、基于一致性的束算法 CBBA。
- 后续补充：对有规范中文全称的算法/系统方法名也在首现处展开，新增 12 处：VINS-Mono（视觉-惯性导航系统）、LOAM（激光里程计与建图）、FAST-LIO2 / Faster-LIO（快速激光-惯性里程计）、LVI-SAM（激光-视觉-惯性同步定位与建图）、Faster R-CNN（基于区域卷积神经网络）、SSD（单发多框检测器）、DETR（检测Transformer）、ViT（视觉Transformer）、Deep SVDD（深度单类支持向量数据描述）、CLIP（对比语言-图像预训练）、MVTec AD（MVTec异常检测）。
- 仍保留原名不展开（无通用中文全称的专有模型/数据集名）：YOLO、Deformable DETR、Swin Transformer、MobileNet、EfficientNet、PatchCore、PaDiM、FastFlow、EfficientAD、CutPaste、DRAEM、WinCLIP、ORB-SLAM、AutoVI、VisA、ImageNet 等。CNN/VAE/GAN 等文中仅用中文全称（卷积神经网络、变分自编码器、生成对抗网络），无缩写故不处理。
- 重新编译 `xelatex → bibtex → xelatex × 2` 通过，41 页，无未定义引用。
